key = "execution.grouping.job"
type = "enum"
storage = "node"
default = "owner"
environment = "KERNEL_EXECUTION_GROUPING_JOB"
allowed = ["isolated", "owner", "namespace", "shared"]
runtime_mutable = false
restart_required = true
description = "Default runtime-group strategy for jobs."
