key = "logging.enabled"
type = "boolean"
storage = "node"
default = true
environment = "THE8020_LOGGING_ENABLED"
runtime_mutable = true
restart_required = false
description = "Whether managed kernel and Deno logs are persisted. Existing logs remain readable when disabled."
