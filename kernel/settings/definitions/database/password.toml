key = "database.password"
type = "string"
storage = "global"
default = ""
environment = "KERNEL_DATABASE_PASSWORD"
runtime_mutable = false
restart_required = true
description = "PostgreSQL password; ignored by SQLite. Stored as a plain configuration value during kernel development."
