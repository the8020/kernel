key = "runtime.sandbox.target_ram_utilization_percent"
type = "integer"
storage = "node"
default = 80
environment = "KERNEL_RUNTIME_SANDBOX_TARGET_RAM_UTILIZATION_PERCENT"
minimum = 1
maximum = 100
runtime_mutable = false
restart_required = true
description = "RAM utilization percentage at or above which a sandbox receives no additional Workers."
