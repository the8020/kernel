key = "runtime.sandbox.stop_grace_period"
type = "integer"
storage = "node"
default = 10000
environment = "THE8020_RUNTIME_SANDBOX_STOP_GRACE_PERIOD"
minimum = 100
maximum = 300000
runtime_mutable = false
restart_required = true
description = "Grace period before a sandbox task is killed, in milliseconds."
