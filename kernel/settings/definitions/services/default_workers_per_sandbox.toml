key = "services.default_workers_per_sandbox"
type = "integer"
storage = "node"
default = 4
environment = "KERNEL_SERVICES_DEFAULT_WORKERS_PER_SANDBOX"
minimum = 1
maximum = 1000000
runtime_mutable = false
restart_required = true
description = "Default per-service Worker packing limit in one sandbox."
