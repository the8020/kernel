key = "services.reconcile_interval"
type = "integer"
storage = "node"
default = 1000
environment = "THE8020_SERVICES_RECONCILE_INTERVAL"
minimum = 100
maximum = 600000
runtime_mutable = false
restart_required = true
description = "Active filesystem-service runtime maintenance interval in milliseconds; package catalog discovery is not periodic."
